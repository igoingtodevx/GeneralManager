{
  "name": "general-manager",
  "private": true,
  "version": "2.1.0",
  "scripts": { "test": "node --test tests/*.test.cjs", "check": "node --check app.js && node --check db.js && node --check domain.js && node --check auth.js" }
}
